{% block body %}
    <h1>Create New Blog</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" >Create</button>
    {{ form_end(form) }}
    <a href="{{ path('blogs_all') }}">Back to list</a>
{% endblock %}
